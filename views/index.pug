extends layout.pug

block content
    // content is like yield 
    section#content 
    
      h2 Welcome, #{name}!
      form(action='/goodbye', method='post')
        button(type='submit') Goodbye
    //h2 Welcome, student!
